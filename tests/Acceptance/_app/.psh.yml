header: |
  SHOPWARE PHP-SH

paths:
  - scripts/

const:
  ENV: prod
  TEST: prod
  HOST: http://www.selfish.de

dynamic:
  ID: id
  LL: ls -ahl

environments:
  test:
    paths:
      - "envpath/"
    const:
      ENV: "test"